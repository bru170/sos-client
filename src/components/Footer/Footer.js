import React from "react"
import styles from "./Footer.module.scss"
const Footer = () => (
  <footer className={styles.footer}>
    <div className={styles.grid}>
      <div className={styles.grid__items}></div>
    </div>
  </footer>
)

export default Footer
